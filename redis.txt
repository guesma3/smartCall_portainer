version: '3'

services:
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--requirepass", "k0kTU^RM@39C"]
    networks:
      - portainer_network

volumes:
  redis_data:

networks:
  portainer_network:
    external: true
